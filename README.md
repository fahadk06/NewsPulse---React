Note:
This app uses NewsAPI, whose free tier restricts API calls to localhost only. The GitHub Pages deployment above will show a blank screen due to this restriction — to see it working, clone the repo and run locally with npm start.
